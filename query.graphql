

query ProgramSet($id: ID!) {
  programSet(id: $id) {
    title
    id
    lastItemModified

    items {
      nodes {
        title
        summary
        id
        publishDate
        audios {
          title
          url
          mimeType
        }
      }
    }
  }
}