name: Release

on:
  push:

jobs:
  lints:
    name: Build
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v4
        with:
          init: none
          planner: linux
      # - name: Run `nix build`
      #   run: nix build .
